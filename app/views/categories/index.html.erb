<h1>Categories#index</h1>
<p>Find me in app/views/categories/index.html.erb</p>

<%@categories.each do |category|%>
  <h4><%= link_to category.name, category_path(category), class: category.color %></h4>
<% end %>
<%@articles.each do |article|%>
  <%= article.url  %>
 <% end %>
